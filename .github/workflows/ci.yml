name: CI

on:
  pull_request:
    branches:
      - develop

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Restore dependencies
        run: dotnet restore ./TaskTrackR.Api/TaskTrackR.Api.csproj

      - name: Build
        run: dotnet build ./TaskTrackR.Api/TaskTrackR.Api.csproj --no-restore --configuration Release

      - name: Test
        run: echo "ToDo, build test project"

